name: Backend CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'backend/**'
  pull_request:
    branches:
      - main
    paths:
      - 'backend/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: ./backend

      - name: Run ESLint Security Scan
        run: npm run lint
        working-directory: ./backend

      - name: Run backend tests
        run: npm test
        working-directory: ./backend

  deploy:
    needs: build-and-test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Deploy to Render
        # Replace with your actual Render deployment action or script
        # This is a placeholder. You would typically use Render's API or a custom script.
        run: echo "Deploying backend to Render..."
        # You would add your Render deployment logic here, e.g.:
        # - uses: some-render-deployment-action@v1
        #   with:
        #     service-id: ${{ secrets.RENDER_SERVICE_ID }}
        #     api-key: ${{ secrets.RENDER_API_KEY }}
