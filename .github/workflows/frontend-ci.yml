name: Frontend CI/CD

on:
  push:
    branches:
      - main
    paths:
      - 'frontend/**'
  pull_request:
    branches:
      - main
    paths:
      - 'frontend/**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm install
        working-directory: ./frontend

      - name: Build frontend
        run: npm run build
        working-directory: ./frontend

  deploy:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    steps:
      - name: Deploy to Vercel
        # Replace with your actual Vercel deployment action or script
        # You would typically use the Vercel CLI or an action.
        run: echo "Deploying frontend to Vercel..."
        # Example for Vercel CLI deployment:
        # - name: Install Vercel CLI
        #   run: npm install --global vercel@latest
        # - name: Deploy to Vercel
        #   run: vercel deploy --prebuilt --prod --token=${{ secrets.VERCEL_TOKEN }}
        #   working-directory: ./frontend/dist # Or wherever your build output is
